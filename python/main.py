from python.dami.experiments import gender
from python.dami.experiments import gender_2
from python.dami.experiments import heart
from python.dami.experiments import lymphography
from python.dami.experiments import spambase

if __name__ == '__main__':
    # gender.run_experiment()

    # gender_2.run_experiment(k=3)

    spambase.run_experiment(k=5)

    # heart.run_experiment(k=1)
    # heart.run_experiment(k=2)
    # heart.run_experiment(k=5)
    # heart.run_experiment(k=10)

    # lymphography.run_experiment(k=1)
    # lymphography.run_experiment(k=2)
    # lymphography.run_experiment(k=5)
    # lymphography.run_experiment(k=10)
